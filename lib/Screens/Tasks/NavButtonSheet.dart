import 'package:flutter/material.dart';

class NavButtonSheet extends StatelessWidget {
  const NavButtonSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
